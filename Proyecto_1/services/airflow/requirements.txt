# Dependencias para procesamiento de datos
pandas==2.2.3
scikit-learn==1.5.2
numpy==1.26.4

# Cliente HTTP para comunicación con MLflow y API externa
requests==2.32.3
httpx==0.27.2

# Conectores de base de datos
psycopg2-binary==2.9.9

# Providers de Airflow
apache-airflow-providers-postgres==5.12.0
apache-airflow-providers-http==4.12.0

# Cliente de MLflow (solo para tracking, no el servidor completo)
mlflow-skinny==2.16.2

# Utilidades adicionales
python-dotenv==1.0.1